[package]
name = "nice-project"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.1"
sqlx = { version = "0.8.3", features = ["sqlite", "runtime-tokio", "tls-rustls-aws-lc-rs"] }
tokio = { version = "1.43.0", features = ["rt", "rt-multi-thread", "macros"] }
